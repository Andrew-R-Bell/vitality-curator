#Procfile

#Note: A Procfile is used in Railway (or Heroku) to define process types. 
#For this bot, include a line like worker: python main.py to run the bot as a worker process for automatic daily posting.

worker: python main.py